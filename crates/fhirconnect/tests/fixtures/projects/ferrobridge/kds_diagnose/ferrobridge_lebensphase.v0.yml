# SPDX-FileCopyrightText: Vernum Projecten B.V.
# SPDX-License-Identifier: Apache-2.0
# A modified copy of model/cluster/org.highmed/lebensphase.v0.yml, a
# derivative of it under its licence.
# Source: SevKohler/FHIRconnect-mapping-lib at commit 6bd4c19a (vendored
# under docs/specs/fhirconnect-mapping-lib), Apache License 2.0; the source
# file carries no copyright line to keep.
# Changed: line 15 writes `mappings:` with no value, a null the published model
# schema types as an array and the strict loader refuses (#101); the empty
# list is the one change, so the published KDS_lebensphase extension has the
# model it extends.
grammar: FHIRConnect/v1.0.0
type: model
metadata:
  name: CLUSTER.lebensphase.v0
  version: 0.0.1-alpha
spec:
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-CLUSTER.lebensphase.v0
    revision: 0.0.1-alpha
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/CodeableConcept

mappings: []

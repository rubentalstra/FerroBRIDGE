# SPDX-FileCopyrightText: Vernum Projecten B.V.
# SPDX-License-Identifier: BUSL-1.1
# A FerroBRIDGE-authored extension of the published
# EVALUATION.problem_diagnosis.v1, shaped like the diagnosis extension of the
# mapping library and exercising all three extension methods. Every code system
# is example.org, which RFC 6761 section 6.5 reserves for examples.
grammar: FHIRConnect/v1.0.0
type: extension
metadata:
  name: ferrobridge_problem_diagnose
  version: 1.0.0
spec:
  system: FHIR
  version: R4
  extends: EVALUATION.problem_diagnosis.v1

mappings:
  - name: "problemQualifier"
    extension: "add"
    with:
      fhir: "$resource"
      openehr: "$archetype/data[at0001]/items[openEHR-EHR-CLUSTER.problem_qualifier.v2]"
    slotArchetype: "CLUSTER.problem_qualifier.v2"

  - name: "assertedDate"
    extension: "add"
    unidirectional: "openehr->fhir"
    with:
      fhir: "$resource.recordedDate"
      openehr: "$archetype/data[at0001]/items[at0003]"
      type: "DATETIME"

  - name: "codedProblemDiagnose"
    extension: "append"
    appendTo: "problemDiagnose"
    followedBy:
      mappings:
        - name: "coding"
          with:
            fhir: "coding"
            openehr: "$openehrRoot"
          fhirCondition:
            targetRoot: "coding"
            targetAttribute: "system"
            operator: "one of"
            criteria: "http://example.org/fhir/CodeSystem/ferrobridge-diagnosis"
          followedBy:
            mappings:
              - name: "codingSystem"
                with:
                  fhir: "$fhirRoot"
                manual:
                  - name: "system"
                    fhir:
                      - path: "system"
                        value: "http://example.org/fhir/CodeSystem/ferrobridge-diagnosis"

              - name: "certainty"
                with:
                  fhir: "extension"
                  openehr: "$archetype/data[at0001]/items[at0073]"
                fhirCondition:
                  targetRoot: "extension"
                  targetAttribute: "url"
                  operator: "one of"
                  criteria: "http://example.org/fhir/StructureDefinition/diagnostic-certainty"
                followedBy:
                  mappings:
                    - name: "certaintyValue"
                      with:
                        fhir: "value.ofType(Coding)"
                        openehr: "$archetype/data[at0001]/items[at0073]"
                        type: "CODING"

  # The published method writes "$archetype/diagnose/other_participations" and
  # EVALUATION has no "diagnose" attribute; ENTRY.other_participations is the
  # RM attribute it means (openEHR RM 1.1.0, ENTRY). Its manual `function` and
  # its untyped performer write tails no FLAT part carries, so the method is
  # written in the participationsFunction form concept-mappings.adoc gives for
  # it. Recorded on #101.
  - name: "other_participations"
    extension: "overwrite"
    with:
      fhir: "$resource.asserter"
      openehr: "$archetype/other_participations"
    participationsFunction: "asserter"

  # The published method maps the asserter into EVENT_CONTEXT.participations
  # through the same manual `function` and untyped performer; this extension
  # writes the context participation in the participationsFunction form.
  - name: "participations"
    extension: "overwrite"
    with:
      fhir: "$resource.asserter"
      openehr: "$composition/context/participations"
    participationsFunction: "asserter"

  # The published method maps bodySite onto `$archetype/data[at0012]`, which
  # the synthetic template carries as no node, so the tail is `data` below the
  # EVALUATION and no FLAT part carries it; bodySiteCluster maps the body site.
  - name: "bodySite"
    extension: "overwrite"
    with:
      fhir: "$resource"
      openehr: "$archetype"
      type: "NONE"

  - name: "bodySiteCluster"
    extension: "overwrite"
    with:
      fhir: "$resource.bodySite.text"
      openehr: "$archetype/data[at0012]/items[at0013]"
      type: "STRING"

  - name: "dateTime"
    extension: "overwrite"
    with:
      fhir: "$resource"
      openehr: "$archetype"
      type: "NONE"
    followedBy:
      mappings:
        - name: "onset"
          with:
            fhir: "onset.ofType(dateTime)"
            openehr: "data[at0001]/items[at0077]"
            type: "DATETIME"
          openehrCondition:
            targetRoot: "$archetype/data[at0001]"
            targetAttributes:
              - "items[at0077]"
            operator: "not empty"

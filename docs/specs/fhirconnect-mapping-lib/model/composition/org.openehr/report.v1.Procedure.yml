grammar: FHIRConnect/v1.0.0
type: model
metadata:
  name: COMPOSITION.report.v1.Procedure
  version: 0.0.1-alpha
spec:
  system: FHIR
  version: R4
  openEhrConfig:
    archetype: openEHR-EHR-COMPOSITION.report.v1
    revision: 1.2.4
  fhirConfig:
    structureDefinition: http://hl7.org/fhir/StructureDefinition/Procedure

mappings:
 #status is mapped in ISM transiition

  - name: "participations"
    with:
      fhir: "$resource.performer.actor"
      openehr: "$composition/context/participations"  # reference to part
      type: "NONE"
    followedBy:
      mappings:
        - name: "participationFunction"
          with:
            fhir: "$fhirRoot"
            openehr: "$openEHRRoot"
          manual:
            - name: "function"
              openehr:
                - path: "function"
                  value: "asserter"
        - name: "performer"
          with:
            fhir: "$fhirRoot"
            openehr: "$openEHRRoot" # reference to party_identified NOT SUPPORTED since 1..1 whole mapping is commented out

  #  reference to party_identified NOT SUPPORTED since 1..1 whole mapping is commented out
  - name: "composer"
    with:
      fhir: "$resource.recorder"
      openehr: "$composition/composer"

  - name: "composerEmpty"
    with:
      fhir: "$resource.recorder"
      openehr: "$composition/composer"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "recorder"
      operator: "empty"
    manual:
      - name: "no information"
        openehr:
          - path: "null_flavour/value"
            value: "no information"
          - path: "null_flavour/defining_code/terminology_id"
            value: "openehr"
          - path: "null_flavour/defining_code/code_string"
            value: "271"

  - name: "context"
    with:
      fhir: "$resource"
      openehr: "$composition/context"
      type: "NONE"
    unidirectional: "fhir->openehr" #action time also exists thats why
    followedBy:
      mappings:
        - name: "performed"
          with:
            fhir: "performed.ofType(Period)"
            openehr: "$reference"
          followedBy:
            mappings:
              - name: "performedEnd"
                with:
                  fhir: "end"
                  openehr: "end_time"

              - name: "performedStart"
                with:
                  fhir: "start"
                  openehr: "start_time"

        - name: "performedDateTime"
          with:
            fhir: "performed.ofType(DateTimeType)"
            openehr: "start_time"

  - name: "contextStartTimeEmpty"
    with:
      fhir: "$resource"
      openehr: "$composition/context"
      type: "NONE"
    unidirectional: "fhir->openehr"
    fhirCondition:
      targetRoot: "$resource"
      targetAttribute: "performed"
      operator: "empty"
    followedBy:
      mappings:
        - name: "performedStart"
          with:
            openehr: "start_time"
          manual:
            - name: "no information"
              openehr:
                - path: "null_flavour/value"
                  value: "no information"
                - path: "null_flavour/defining_code/terminology_id"
                  value: "openehr"
                - path: "null_flavour/defining_code/code_string"
                  value: "271"


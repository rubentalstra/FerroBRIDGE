grammar: FHIRConnect/v1.0.0
type: extension
metadata:
  name: KDS_fall_problem_diagnose
  version: 0.0.1-alpha
spec:
  system: FHIR
  version: R4
  extends: EVALUATION.problem_diagnosis.v1

mappings:

  - name: "contextStartTime" #non-detereministic behaviour bug
    extension: "overwrite"
    with:
      fhir: "$resource.recordedDate"
      openehr: "$composition/context/start_time"
      type: "NONE"
